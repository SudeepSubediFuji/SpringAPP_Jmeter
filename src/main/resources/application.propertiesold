spring.application.name=multiChatClient

# Logging
logging.pattern.console=%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n
logging.level.org.springframework.ai.chat.client.advisor=DEBUG

# Spring AI / Ollama Configuration using Docker hosted ollama
spring.ai.ollama.chat.options.model=gemma3:1b
spring.ai.openai.api-key=${OPENAI_KEY}
# By default, Spring AI autoconfigures a single ChatClient.Builder bean. However, you may need to work with multiple chat models in your application.
# In all cases, you need to disable the ChatClient.Builder autoconfiguration
# This allows you to create multiple ChatClient instances manually.
spring.ai.chat.client.enabled=false

#spring.ai.ollama.base-url=http://localhost:11434

#OpenAI Configuration
#spring.ai.openai.chat.options.model=gpt-4-turbo
#spring.ai.openai.chat.options.temperature=0.7
#spring.ai.openai.chat.options.max-tokens=2048
#spring.ai.openai.chat.options.top-p=0.9
#spring.ai.openai.chat.options.frequency-penalty=0.0
#spring.ai.openai.chat.options.presence-penalty=0.0
#spring.ai.openai.chat.options.stream=true

#JDBC Configuration for Spring Session
# Here we are using H2 database for simplicity. In production, consider using a more robust database like PostgreSQL or MySQL.
# The database file will be created in the user's home directory namely 'chatMemory'
# we can check the database with table-name="ai_chat_memory" which will save our chat using CONVERSATION_ID

spring.datasource.url=jdbc:h2:file:C:/Users/SudeepFuji/chatMemory;AutoServer=true;
spring.datasource.username=sudeep
spring.datasource.password=12345
spring.datasource.driver-class-name=org.h2.Driver
spring.ai.chat.memory.repository.jdbc.initialize-schema=always
spring.ai.chat.memory.repository.jdbc.schema=classpath:/schema/schema-h2db.sql

#spring.ai.chat.memory.repository.jdbc.platform
#Mysql Configuration Example
#spring.datasource.url=jdbc:mysql://localhost:3306/
#spring.datasource.username=root
#spring.datasource.password=4322
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver